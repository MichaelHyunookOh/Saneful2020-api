CREATE TABLE saneful_user (
    user_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user_name TEXT NOT NULL UNIQUE,
    user_email TEXT,
    user_password TEXT NOT NULL,
    date_created TIMESTAMPTZ DEFAULT now() NOT NULL
);
